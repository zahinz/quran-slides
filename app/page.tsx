import GenerateSlideForm from './components/GenerateSlideForm';

const Home = (): React.JSX.Element => {
  return (
    <div className="flex flex-col items-center">
      <GenerateSlideForm />
    </div>
  );
};

export default Home;
